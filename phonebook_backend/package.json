{
  "name": "phonebook_backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rm -rf dist && cd ../../part2/the_phonebook/ && npm run build && cp -r dist ../../part3/phonebook_backend/",
    "deploy": "flyctl deploy",
    "deploy:full": "yarn build:ui && yarn deploy",
    "logs:prod": "flyctl logs"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^6.7.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
